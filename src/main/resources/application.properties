spring.application.name=trading-app

# Database connection using project-specific env variables
spring.datasource.url=${TRADING_DB_URL}
spring.datasource.username=${TRADING_DB_USER}
spring.datasource.password=${TRADING_DB_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.config.import=optional:file:.env[.properties]
# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true